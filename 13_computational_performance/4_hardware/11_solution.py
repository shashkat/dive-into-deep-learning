# Having Direct Memory Access (DMA) allows the CPU to be able to perform other tasks while the 
# data transfer is happening between the external device and the memory. Without DMA, the CPU 
# is occupied for the whole duration of the data transfer. Hence, we save on time by using DMA.